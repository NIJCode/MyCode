#calcule avec une fonction

b = float(input('Choisir la valeur de B = '))
c = float(input('choisir la valeur de C = '))   
d = float(input('choisir la valeur de D = '))

def a(b, c ,d):   
    return ((3*b+c)/d)
print(a)


# Def nom de la fonction(param1, param2, ...):
# 
#       Instruction de la fonction
# 
# return résultat1, résultats

